<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.example.afreecaandroid.ui.viewmodel.TalkCamViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.view.TalkCamDetailFragment">

        <ImageView
            android:id="@+id/iv_detail_bj_profile_image"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_marginTop="140dp"
            setProfileImageAtDetail="@{viewModel.talkCamBroadCastDetail}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tv_detail_broadcast_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            setTitleAtDetail="@{viewModel.talkCamBroadCastDetail}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/iv_detail_bj_profile_image"
            tools:text="제목" />

        <TextView
            android:id="@+id/tv_detail_bj_nickname"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            setNickNameAtDetail="@{viewModel.talkCamBroadCastDetail}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tv_detail_broadcast_title"
            tools:text="닉네임" />

        <ImageView
            android:id="@+id/iv_detail_thumb_nail"
            android:layout_width="220dp"
            android:layout_height="120dp"
            android:layout_marginTop="50dp"
            setThumbnailImageAtDetail="@{viewModel.talkCamBroadCastDetail}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tv_detail_bj_nickname" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
